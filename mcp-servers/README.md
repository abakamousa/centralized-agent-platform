# MCP Servers

This directory contains independently deployable Model Context Protocol servers.

Each server:

- owns its own runtime and dependencies
- validates forwarded user context
- exposes a narrow tool boundary
- can be built and deployed separately
