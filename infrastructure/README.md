# Infrastructure

Terraform code for provisioning the shared cloud environment, including:

- Azure Container Apps environment
- Azure Cosmos DB
- role assignments and managed identities
- networking and observability foundations

Environment-specific entry points live under `environments/`.
